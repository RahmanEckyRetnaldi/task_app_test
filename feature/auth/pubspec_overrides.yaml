# melos_managed_dependency_overrides: common_dependency,components,env,nav_list,network
dependency_overrides:
  common_dependency:
    path: ../../core/common_dependency
  components:
    path: ../../core/components
  env:
    path: ../../env
  nav_list:
    path: ../../core/nav_list
  network:
    path: ../../core/network
